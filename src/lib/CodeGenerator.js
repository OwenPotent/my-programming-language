module.exports = class CodeGenerator {
    // We create the constructor to initialize the class
    constructor(ast) {
        // We initialize the class
        this.ast = ast;
    }

    // We create the generate function to generate the code from the AST
    generate() {}
}